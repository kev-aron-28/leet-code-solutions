# Arrays

## Que es un arreglo?
Un array es una estructura de datos que guarda elementos del mismo tipo en memoria contigua.

## Memoria contigua
Cuando declaras un array, el sistema reserva un bloque seguido de memoria.
